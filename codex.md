# BYD CASHFLOW — เอกสารส่งต่องาน

อัปเดตล่าสุด: 28 กรกฎาคม 2026

เอกสารนี้ใช้สำหรับเปิดงานต่อหลังย้ายคอม และบันทึกงานขั้นสุดท้ายที่ต้องทำเมื่อโปรแกรมเสร็จสมบูรณ์

## ที่เก็บโค้ด

- GitHub: `https://github.com/suphasin01/BYDCASHFLOW.git`
- Branch หลัก: `main`
- Remote ต้องเป็น `origin`
- ห้ามเพิ่มระบบอัปเดตเวอร์ชันอัตโนมัติ
- ห้ามสร้าง GitHub Release หรือ workflow ที่ปล่อยตัวติดตั้งอัตโนมัติ
- ห้ามเปลี่ยนเลขเวอร์ชัน เว้นแต่ผู้ใช้สั่งโดยตรง

## เทคโนโลยี

- Electron: โปรแกรม Desktop
- React + TypeScript + Vite: หน้าจอ
- Express: Local API ที่ `http://localhost:3737`
- SQLite (`better-sqlite3`): ฐานข้อมูลในเครื่อง
- electron-builder: สร้างตัวติดตั้ง Windows

## ตำแหน่งสำคัญ

- `src-ui/src/` — หน้าจอโปรแกรม
- `src-ui/src/pages/` — หน้าต่าง ๆ
- `src/api.ts` — API
- `src/db.ts` — ตารางและคำสั่งฐานข้อมูล
- `electron/main.js` — การทำงานของ Electron
- `web/` — UI ที่ build แล้ว
- `dist/` — Backend ที่ compile แล้ว
- `release/` — ตัวติดตั้งที่สร้างในเครื่อง (ไม่ push)

ฐานข้อมูลจริงของผู้ใช้เก็บที่:

```text
C:\Users\<ชื่อผู้ใช้>\.local-api\data.db
```

ไฟล์ฐานข้อมูลไม่อยู่ใน Git ต้องสำรองแยกก่อนย้ายคอม หากต้องการย้ายข้อมูลลูกค้าและเอกสารเดิม

## เปิดงานต่อบนคอมใหม่

ติดตั้งก่อน:

1. Git
2. Node.js รุ่น LTS
3. Visual Studio Build Tools พร้อม Desktop development with C++

จากนั้นรัน:

```powershell
git clone https://github.com/suphasin01/BYDCASHFLOW.git
cd BYDCASHFLOW
npm install
cd src-ui
npm install
cd ..
npm run build:ui
npm run build
```

เปิดโปรแกรมสำหรับพัฒนา:

```powershell
npm run electron
```

ตรวจสอบ UI อย่างเดียว:

```powershell
cd src-ui
npm run dev
```

Local API ใช้พอร์ต `3737`

## ก่อนเริ่มแก้งานทุกครั้ง

```powershell
git pull origin main
git status
```

หลังแก้งาน:

```powershell
npm run build
cd src-ui
npm run build
cd ..
git status
git add <ไฟล์ที่แก้>
git commit -m "ข้อความสรุปงาน"
git push origin main
```

ต้องตรวจทั้ง TypeScript backend และ UI ก่อน push และต้องไม่ทับไฟล์งานอื่นที่ยังไม่ได้ commit

## สถานะฟังก์ชันหลัก

- ชื่อโปรแกรม BYD CASHFLOW
- ใช้งานแบบ Local/Offline
- เอกสารหลัก:
  - ใบเสร็จรับเงิน/ใบกำกับภาษี
  - ใบส่งของ
  - ใบสั่งงาน
- ใบสั่งงานเชื่อมกับใบส่งของ
- สินค้าเชื่อมเข้าแบบฟอร์มเอกสาร
- ใบสั่งงานรองรับรูปแบบป้าย
- ระบบรับชำระและยอดค้าง
- สรุปยอดชำระลูกค้ารายเดือน
- ยกยอดค้างชำระจากเดือนก่อน
- แก้ไขประวัติรับชำระ
- พิมพ์และบันทึก PDF
- ไม่มีระบบ GitHub Release และไม่มี Auto Update

## งานขั้นสุดท้ายหลังโปรแกรมเสร็จ

เมื่อผู้ใช้ยืนยันว่าแก้งานทั้งหมดเสร็จแล้ว ให้ทำตามลำดับนี้:

1. ทดสอบทุกหน้าและทุกเอกสาร
2. สำรองฐานข้อมูล
3. สร้างตัวติดตั้ง Windows ด้วย `npm run dist:win`
4. ติดตั้งบนเครื่องลูกค้าทดสอบ
5. ทำระบบสิทธิ์ใช้งานแบบล็อกตามเครื่อง
6. ทดสอบถอนและติดตั้งใหม่บนเครื่องเดิม
7. ทดสอบว่าคัดลอกโปรแกรมหรือฐานข้อมูลไปอีกเครื่องแล้วเปิดใช้งานไม่ได้
8. เก็บรหัสกู้คืนและข้อมูลออกใบอนุญาตไว้กับเจ้าของโปรแกรม

## แนวทางล็อกโปรแกรมตามเครื่อง

ยังไม่ต้องทำจนกว่าโปรแกรมจะเสร็จและผู้ใช้สั่งเริ่มงานนี้

แนวทางที่เลือกไว้:

- หนึ่งใบอนุญาตต่อหนึ่งเครื่อง
- สร้าง Machine ID จากข้อมูลเครื่องหลายค่า แล้ว hash ก่อนใช้งาน
- ห้ามเก็บ Serial Number ดิบหรือข้อมูลฮาร์ดแวร์ส่วนตัวลง GitHub
- สร้างไฟล์ License ที่มี:
  - รหัสลูกค้า
  - Machine ID แบบ hash
  - วันที่ออกใบอนุญาต
  - สถานะใบอนุญาต
  - ลายเซ็นดิจิทัล
- โปรแกรมฝังเฉพาะ Public Key สำหรับตรวจลายเซ็น
- Private Key สำหรับออก License ต้องเก็บนอกโปรเจกต์และห้าม commit
- ตรวจ License ตอนเปิดโปรแกรมก่อนเปิดหน้าหลัก
- หาก Machine ID ไม่ตรง ให้แสดงหน้าขอเปิดใช้งาน ไม่ให้เข้าข้อมูลธุรกิจ
- เตรียมขั้นตอนย้ายสิทธิ์เมื่อเครื่องเสียหรือเปลี่ยนเครื่อง

ไม่ควรล็อกด้วย Gmail เพียงอย่างเดียว เพราะลูกค้าสามารถนำบัญชีไปเข้าสู่ระบบเครื่องอื่นได้ การผูก Machine ID และลายเซ็นดิจิทัลเหมาะกับการจำกัดให้ใช้ได้เครื่องเดียวมากกว่า

## การสร้าง Setup

สร้างในเครื่องเท่านั้น:

```powershell
npm run dist:win
```

ไฟล์จะอยู่ใน `release/` และต้องไม่ push ไฟล์ติดตั้งขึ้น GitHub

หากต้องการแบบ Portable:

```powershell
npm run dist:win-portable
```

ก่อนส่งให้ลูกค้า ต้องยืนยันว่า:

- ไม่มี Auto Update
- ไม่มีการเชื่อม GitHub หลังติดตั้ง
- เปิดได้โดยไม่ต้องติดตั้ง Node.js
- ฐานข้อมูลยังอยู่หลังอัปเดตหรือติดตั้งโปรแกรมใหม่
- License ใช้ได้เฉพาะเครื่องที่อนุญาต

## ข้อควรระวัง

- ห้าม commit `.env`, Private Key, License master key หรือข้อมูลลูกค้า
- ห้ามลบ `data.db` ระหว่างถอนโปรแกรม
- ห้ามเปลี่ยนโครงสร้างเอกสารโดยไม่ทดสอบตัวอย่างพิมพ์และ PDF
- การแก้ VAT ต้องตรวจทั้งฟอร์ม ยอดรวม แบบร่าง และ PDF
- การแก้ฟอร์มเอกสารต้องแก้ส่วน Preview ให้ตรงกันทุกครั้ง

